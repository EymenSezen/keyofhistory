# Default active profile. For local dev without docker/dependencies, we default to H2 in-memory db.
spring.profiles.active=h2

# Server port (default is 8080, similar to Kestrel port config)
server.port=8080

# Jackson formatting (JSON serializer, similar to System.Text.Json)
spring.jackson.serialization.write-dates-as-timestamps=false
